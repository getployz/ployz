use std::process::ExitCode;

fn main() -> ExitCode {
    eprintln!(
        "ployzd command startup is not implemented yet; use the library surface for substrate tests"
    );
    ExitCode::FAILURE
}
