name: fixture_pre_start_depends_on
services:
  first:
    image: alpine:3.20
    healthcheck:
      test: ["CMD-SHELL", "true"]
  second:
    image: alpine:3.20
    depends_on:
      first:
        condition: service_healthy
    pre_start:
      command: ["sh", "-c", "echo ready"]
