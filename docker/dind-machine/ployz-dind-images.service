[Unit]
Description=Load baked workload image tarballs into the inner Docker daemon
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c 'for tar in /images/*.tar; do docker load -i "$tar"; done'

[Install]
WantedBy=multi-user.target
