---
title: Repo Sources
description: Source files that ground these docs.
llms:
  summary: Important source files for documentation.
---

These docs are grounded in repo sources:

- `VISION.md` for product direction and primitive surface.
- `docs/architecture.md` for architecture and state model.
- `crates/ployzctl/src/main.rs` for the current native `ployzctl` entrypoint.
- `crates/ployzd/src/cli.rs` for the current forwarded daemon command surface.

When docs and source disagree, update the docs or generate the reference from
source.
