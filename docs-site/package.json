{
  "name": "@ployz/docs-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "playwright install chromium && astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.0",
    "@astrojs/starlight": "^0.39.2",
    "astro": "^6.3.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "rehype-mermaid": "^3.0.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
